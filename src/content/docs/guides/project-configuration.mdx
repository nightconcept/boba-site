---
title: Project Configurtion
---

## Project TOML

```toml
# boba.toml

[project]
name = "my_awesome_app"
version = "0.1.0"
# Could define an entry point, e.g., main = "src/main.boba"

# This section is for the future, but defines the path forward
[dependencies]
# "github.com/boba-lang/http" = "1.0"
```